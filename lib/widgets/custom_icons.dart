import 'package:flutter/material.dart';

class CustomIcons {
  static Widget zap({double size = 24, Color? color}) {
    return Icon(Icons.flash_on, size: size, color: color);
  }

  static Widget flame({double size = 24, Color? color}) {
    return Icon(Icons.local_fire_department, size: size, color: color);
  }

  static Widget compass({double size = 24, Color? color}) {
    return Icon(Icons.explore, size: size, color: color);
  }

  static Widget barChart({double size = 24, Color? color}) {
    return Icon(Icons.bar_chart, size: size, color: color);
  }

  static Widget play({double size = 24, Color? color}) {
    return Icon(Icons.play_arrow, size: size, color: color);
  }

  static Widget chevronRight({double size = 24, Color? color}) {
    return Icon(Icons.chevron_right, size: size, color: color);
  }

  static Widget heart({double size = 24, Color? color}) {
    return Icon(Icons.favorite, size: size, color: color);
  }

  static Widget trophy({double size = 24, Color? color}) {
    return Icon(Icons.emoji_events, size: size, color: color);
  }

  static Widget clock({double size = 24, Color? color}) {
    return Icon(Icons.access_time, size: size, color: color);
  }

  static Widget mapPin({double size = 24, Color? color}) {
    return Icon(Icons.location_on, size: size, color: color);
  }

  static Widget calendar({double size = 24, Color? color}) {
    return Icon(Icons.calendar_today, size: size, color: color);
  }

  static Widget moreHorizontal({double size = 24, Color? color}) {
    return Icon(Icons.more_horiz, size: size, color: color);
  }

  static Widget activity({double size = 24, Color? color}) {
    return Icon(Icons.directions_run, size: size, color: color);
  }

  static Widget target({double size = 24, Color? color}) {
    return Icon(Icons.track_changes, size: size, color: color);
  }
}
